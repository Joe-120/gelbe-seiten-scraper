# Runtime configuration for Gelbe Seiten Scraper
QUERY=hotel
LOCATION=Berlin
MAX_PAGES=5
CONCURRENCY=5
OUTPUT_DIR=./data/outputs
OUTPUT_FORMATS=json,csv,xlsx
LOG_LEVEL=info

Gelbe Seiten Scraper/docker/Dockerfile
dockerfile# Simple Dockerfile to run the scraper
FROM node:20-alpine

WORKDIR /app
COPY package.json tsconfig.json ./
RUN npm ci --omit=dev

# Copy source and config
COPY src ./src
COPY data ./data
COPY .env.example ./